# How to run
- Run this command on anacondaPrompt at the path of folder
```bash
streamlit run streamli.py --server.maxUploadSize 500
```
- Run once to install all required libraries
```bash
pip install pandas
pip install numpy
pip install scikit-learn
pip install xgboost
pip install imbalanced-learn
pip install joblib
pip install shap
pip install ollama
pip install streamlit
```
